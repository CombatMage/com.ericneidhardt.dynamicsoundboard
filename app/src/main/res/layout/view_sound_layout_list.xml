<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
		xmlns:android="http://schemas.android.com/apk/res/android"
		android:layout_width="match_parent"
		android:layout_height="match_parent"
		android:orientation="vertical"
		android:background="@color/background">

	<RelativeLayout
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:layout_marginBottom="@dimen/margin_small">

		<TextView
				android:id="@+id/tv_sound_layout_header"
				android:layout_width="match_parent"
				android:layout_height="wrap_content"
				android:padding="@dimen/margin_default"
				android:background="@color/primary_200"
				android:textSize="@dimen/text_sub_text"
				android:textStyle="bold"
				android:textAllCaps="true"
				android:text="@string/navigation_drawer_select_sound_layout"/>

		<View
				android:layout_width="match_parent"
				android:layout_height="4dp"
				android:layout_alignBottom="@id/tv_sound_layout_header"
				android:background="@color/accent_200"/>

	</RelativeLayout>

	<android.support.v7.widget.RecyclerView
			android:id="@+id/rv_sound_layouts_list"
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:overScrollMode="never"/>

	<View
			android:layout_width="match_parent"
			android:layout_height="@dimen/height_shadow"
			android:background="@drawable/ic_shadow_horizontal"/>

</LinearLayout>